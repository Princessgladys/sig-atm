<%= form_for(@lokasi_atm) do |f| %>
  <% if @lokasi_atm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lokasi_atm.errors.count, "error") %> prohibited this lokasi_atm from being saved:</h2>

      <ul>
      <% @lokasi_atm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama_lokasi %><br />
    <%= f.text_field :nama_lokasi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
