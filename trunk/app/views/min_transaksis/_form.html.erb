<%= form_for(@min_transaksi) do |f| %>
  <% if @min_transaksi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@min_transaksi.errors.count, "error") %> prohibited this min_transaksi from being saved:</h2>

      <ul>
      <% @min_transaksi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :min_transaksi %><br />
    <%= f.text_field :min_transaksi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
